Iterator is a special class which has next and done methods. It runs for Iterable class.
In Python, while an Iterable class contains __iter__ method, Iterator class contains __next__
and __iter__ methods. The iterable class can be iterated in a loop.